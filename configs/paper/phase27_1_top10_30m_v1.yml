# ========================================
# PHASE27-0: Trade Activity Diagnosis - Multi-Symbol Top10 30m
# 목표: Drop-off instrumentation 검증 (PHASE26-2 기반)
# ========================================

# ========================================
# Universe Provider 설정 (PHASE26-2)
# ========================================
universe:
  enabled: true  # ⭐ Universe 모드 활성화
  
  provider:
    type: topn_volume  # 'static' | 'topn_volume'
    top_n: 10           # Top10 심볼 선정
    cache_ttl_sec: 3600  # 1시간 캐시 (프로세스 시작 시 1회만)
  
  filters:
    quote_assets:
      - USDT               # USDT 쌍만
    exclude_symbols:
      - BTCDOWNUSDT
      - BTCUPUSDT
      - ETHDOWNUSDT
      - ETHUPUSDT          # 레버리지 토큰 제외
    min_24h_volume_usd: 10000000  # 최소 24H 거래량 (10M USDT)
    market_types:
      - PERPETUAL          # 선물 마켓만 (or [SPOT])
    contract_status: TRADING  # TRADING 상태만

# ========================================
# 실행 모드 & 환경
# ========================================
mode: paper
env: paper
run_id: "phase27_0_top10_30m"

# ⭐ symbol 키는 fallback용으로 유지 (universe 실패 시)
symbol: BTCUSDT

timeframe: 5m
lookback: 1000

# ========================================
# Paper Trading 설정
# ========================================
paper:
  duration_mode: "wall_clock"  # 실제 시계 기준
  duration_hours: 0.5           # 30분 실행 (PHASE27-0 Diagnosis용)
  clean_start: true             # 깨끗한 시작 (기존 포지션 무시)

# ========================================
# Capital (FlowGuardian 필수)
# ========================================
capital:
  initial: 10000

# ========================================
# Execution (FlowGuardian 필수)
# ========================================
execution:
  order_type: MARKET
  timeout_sec: 30
  reject_cooldown_seconds: 60

# ========================================
# Equity (엔진 필수)
# ========================================
equity: 10000

# ========================================
# Portfolio 설정 (Multi-Symbol용 보수적 설정)
# ========================================
portfolio:
  initial_balance: 10000.0
  currency: USDT
  max_open_positions: 10       # Top10 × 1포지션/심볼 가정
  max_exposure_pct: 0.6         # 50% → 60% (tuned)
  max_total_exposure: 0.6       # 전체 50% → 60% (PortfolioManager 필수)
  max_strategy_positions: 5     # 전략당 최대 포지션 (보수적)
  use_dynamic_exposure: true
  use_dynamic_budget: true
  symbol_cooldown_seconds: 60

# ========================================
# Position Sizing (Multi-Symbol용 축소)
# ========================================
position_sizing:
  default_risk_per_trade: 0.002  # 0.2% RPT (Multi-Symbol 안전)
  mode: kelly_fraction
  kelly_fraction: 0.25
  leverage: 3.0
  max_leverage: 5.0
  quality_weight_min: 0.7
  quality_weight_max: 1.3
  max_position_value: 5000  # $10k equity * 50%
  min_position_value: 50
  context_scaling:
    enabled: true
    atr_low_pct: 0.004
    atr_high_pct: 0.02
    low_vol_mult: 1.2
    high_vol_mult: 0.7
    neutral_mult: 1.0

# ========================================
# Risk Manager (Multi-Symbol용 강화)
# ========================================
risk:
  max_daily_loss_pct: 0.10      # 10% 일일 손실 한도
  max_consecutive_losses: 10
  cooldown_after_consecutive: 60
  max_portfolio_dd_pct: 0.15    # 15% 포트폴리오 DD 한도
  max_slippage_pct: 0.5
  extreme_loss_cutoff_pct: -20.0
  
  # ⭐ Multi-Symbol 전용 리스크 파라미터
  per_trade: 0.002                # 0.2% RPT (명시적)
  max_positions: 10                # Top10 × 1포지션/심볼
  max_exposure_per_symbol: 0.1    # 심볼당 10% 한도
  max_exposure_pct: 0.6           # 전체 50% → 60% 한도 (tuned)
  
  profiles:
    paper:
      max_daily_loss_pct: 0.20
      max_consecutive_losses: 15
      cooldown_after_consecutive: 30

# ========================================
# Exits (Multi-Symbol용)
# ========================================
exits:
  default_tp_levels:
    - 0.02  # +2%
    - 0.04  # +4%
  default_sl_pct: 0.015  # -1.5%
  trailing_enabled: true
  trailing_activation_pct: 0.01
  trailing_callback_pct: 0.005

# ========================================
# Ensemble 설정 (Score V2 모드)
# ========================================
ensemble:
  enabled: true  # ⭐ Ensemble 모드 활성화
  mode: score_v2  # ⭐ Score V2 엔진 사용
  
  # 5개 V2 전략
  strategies:
    - scalping_v3
    - volatility_breakout_v2
    - mean_reversion_v2
    - trend_follow_v2
    - volume_based_v2
  
  # 전략별 가중치
  strategy_weights:
    scalping_v3: 1.0
    volatility_breakout_v2: 1.0
    mean_reversion_v2: 1.0
    trend_follow_v2: 1.0
    volume_based_v2: 1.0
  
  # 3-Tier Decision Thresholds
  high_conf_threshold: 0.6    # Tier 1: 0.7 → 0.6 (lower barrier)
  consensus_threshold: 0.3    # Tier 2: 0.4 → 0.3 (lower barrier)
  min_strategies: 2
  
  # Dominance Prevention
  max_strategy_weight: 0.6
  
  # Risk/Quality Filters
  max_risk: 0.85              # 0.8 → 0.85 (less strict)
  min_quality: 0.25           # 0.3 → 0.25 (less strict)

# ========================================
# Strategy Selector
# ========================================
strategy:
  selector: ensemble

# ========================================
# FlowGuardian
# ========================================
flow_guardian:
  enabled: true
  selftest:
    max_runtime_sec: 120
    require_metrics:
      - profit_factor
      - winrate
      - score_total
      - exp_score
    min_profit_factor: 0.0
    min_winrate: 0.0
    consistency_checks:
      db_vs_json_score_equal: true
      signals_no_nan: true
      risk_never_oversize: true
    snapshot_rules:
      require_segment_isolation: true
    artifacts:
      require_files:
        - logs/trial_0000.json
        - logs/application.log

# ========================================
# Monitoring
# ========================================
monitoring:
  redis:
    host: localhost
    port: 6379
    db: 0
  metrics:
    enabled: true
    interval_sec: 60

# ========================================
# Database
# ========================================
database:
  url: ${DATABASE_URL}
  host: ${DB_HOST}
  port: ${DB_PORT}
  name: ${DB_NAME}
  user: ${DB_USER}
  password: ${DB_PASSWORD}

# ========================================
# Telegram
# ========================================
telegram:
  token: ${TELEGRAM_TOKEN}
  chat_id: ${TELEGRAM_CHAT_ID}
  enabled: ${ENABLE_TELEGRAM}

# ========================================
# 전략별 파라미터
# ========================================
strategies:
  scalping_v3:
    rsi_oversold: 25          # 30 → 25 (more aggressive)
    rsi_overbought: 75        # 70 → 75 (more aggressive)
    bb_std: 1.8               # 2.0 → 1.8 (tighter bands)
    volume_spike_threshold: 1.3  # 1.5 → 1.3 (lower threshold)
  
  volatility_breakout_v2:
    atr_period: 14
    bb_period: 20
    bb_std: 2.0               # 2.5 → 2.0 (more breakout signals)
    volume_multiplier: 1.3    # 1.5 → 1.3 (lower volume threshold)
  
  mean_reversion_v2:
    bb_period: 20
    bb_std: 1.8               # 2.0 → 1.8 (tighter bands)
    rsi_period: 14
    rsi_oversold: 30          # 25 → 30 (relaxed)
    rsi_overbought: 70        # 75 → 70 (relaxed)
  
  trend_follow_v2:
    fast_ema: 10              # 12 → 10 (faster EMA)
    slow_ema: 24              # 26 → 24 (faster EMA)
    signal_period: 9
    adx_period: 14
    adx_threshold: 20         # 25 → 20 (lower barrier)
  
  volume_based_v2:
    volume_period: 18         # 20 → 18 (faster adaptation)
    volume_spike_threshold: 1.5  # 2.0 → 1.5 (lower threshold)
    obv_period: 14

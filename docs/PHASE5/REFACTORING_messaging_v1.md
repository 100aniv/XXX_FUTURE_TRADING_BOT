# 메시징 시스템 리팩토링 계획 (v1)

**상태 업데이트(2025-11-02)**: PR 1~5 반영 상태 확인 완료(알림 19종 확장, Docker 개별 프로파일 분리, 텔레그램 연동). 아래 개선 과제/테스트는 Phase 6 이후 지속.

## 현재 문제점
- 알림 지연 (현재 1.2초 → 목표 300ms 이하)
- 메시지 중복 전송 문제
- 연결 불안정 시 알림 누락

## 개선 방안
### 1. 우선순위 큐 구현
```python
class PriorityQueue:
    def __init__(self):
        self._queue = []
        self._lock = threading.Lock()
```

### 2. 메시지 추적 시스템
- 전송 이력 해시 저장
- TTL(Time-To-Live) 설정

## 테스트 케이스
| 시나리오 | 검증 방법 |
|----------|-----------|
| 고부하 상황 | 초당 100메시지 처리 테스트 |
| 연결 단절 | 의도적 연결 차단 후 복구 테스트 |

# ============================================
# 백테스트 기간 프리셋
# ============================================
# ⚠️ 실제 데이터 파일명과 정확히 일치해야 함!
# 파일 위치: data/historical/BTCUSDT_5m_START_END.csv
# ============================================

periods:
  # 10년 (기본, 실제 다운로드된 데이터)
  ten_years:
    start_date: "2015-01-01"
    end_date: "2025-10-19"
    description: "10년 전체 데이터 (추천)"
  
  # 1년 (빠른 테스트용)
  one_year:
    start_date: "2024-01-01"
    end_date: "2024-12-31"
    description: "1년 데이터 (별도 다운로드 필요)"
  
  # 3년
  three_years:
    start_date: "2022-01-01"
    end_date: "2024-12-31"
    description: "3년 데이터 (별도 다운로드 필요)"

# 심볼 설정
symbols:
  - BTCUSDT
  - ETHUSDT

# 타임프레임
timeframes:
  - 5m
  - 15m
  - 1h
  - 4h

# 초기 자본
initial_capital: 10000

# 수수료
fee_rate: 0.0004  # 0.04%

# 슬리피지
slippage_pct: 0.0005  # 0.05%
